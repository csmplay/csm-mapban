{
  "name": "csm-mapban",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "packageManager": "yarn@1.22.22",
  "engines": {
    "npm": "use yarn"
  },
  "scripts": {
    "dev": "dotenv -- turbo run dev --parallel -- --turbopack",
    "build": "dotenv -- turbo run build",
    "lint": "dotenv -- turbo run lint",
    "fmt": "prettier --write apps/*/src",
    "start": "dotenv -- turbo run start"
  },
  "keywords": [],
  "author": "CyberSport Masters",
  "license": "AGPL-3.0-only",
  "description": "",
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^2.5.5"
  },
  "dependencies": {
    "dotenv-cli": "^10.0.0",
    "prettier": "^3.6.2"
  }
}
